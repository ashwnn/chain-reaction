# chain-reaction
An in-cluster Kubernetes agent that proves which multi-step attack paths are actually exploitable by executing them, collecting evidence, and mapping validated versus theoretical paths.
