# kind cluster config for the Chain Reaction evaluation environment.
#
# Pinned to match built-in catalog:
#   - single-node cluster
#   - Kubernetes v1.30.4 (digest-pinned)
#   - kind v0.24.0+
#
# Usage:
#   kind create cluster --config deploy/kind-config.yaml

kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
name: chain-reaction-goat
nodes:
  - role: control-plane
    image: kindest/node:v1.30.4@sha256:976ea815844d5fa93be213437e3ff5754cd599b040946b5cca43ca45c2047114
